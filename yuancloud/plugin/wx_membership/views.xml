<?xml version="1.0" encoding="utf-8"?>
<yuancloud>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="wx_membership.list">
      <field name="name">wx_membership list</field>
      <field name="model">wx_membership.wx_membership</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="wx_membership.action_window">
      <field name="name">wx_membership window</field>
      <field name="res_model">wx_membership.wx_membership</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="wx_membership.action_server">
      <field name="name">wx_membership server</field>
      <field name="model_id" ref="model_wx_membership_wx_membership"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->
  
    <!-- Top menu item -->
    <!--
    <menuitem name="wx_membership" id="wx_membership.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="wx_membership.menu_1" parent="wx_membership.menu_root"/>
    <menuitem name="Menu 2" id="wx_membership.menu_2" parent="wx_membership.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="wx_membership.menu_1_list" parent="wx_membership.menu_1"
              action="wx_membership.action_window"/>
    <menuitem name="Server to list" id="wx_membership" parent="wx_membership.menu_2"
              action="wx_membership.action_server"/>
    -->
  </data>
</yuancloud>